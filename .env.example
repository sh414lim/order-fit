# Browser-safe settings. Use these only after replacing the local prototype storage.
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_PUBLISHABLE_KEY=your-publishable-key

# Server/Edge Function only — never expose this value in browser JavaScript.
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
OCR_PROVIDER_API_KEY=your-ocr-provider-key
